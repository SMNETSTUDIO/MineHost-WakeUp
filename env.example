# MineHost Cookie（必需）
# 从浏览器开发者工具中获取
MINEHOST_COOKIE=XXXXXXXXXXX

# 服务器 ID（必需）
MINEHOST_SERVER_ID=XXXXX

# 网页访问密码（可选，默认 admin）
WEB_PASSWORD=admin

# 检查间隔，单位：秒（可选，默认 60）
CHECK_INTERVAL=60

# Flask 密钥（可选，建议在生产环境中设置）
SECRET_KEY=MoLoveSze

# 机器人用户名（可选，默认 WakeUpBot）
BOT_USERNAME=WakeUpBot

# 机器人自动加入（可选，默认 true）
BOT_AUTO_JOIN=true
